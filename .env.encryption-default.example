# OpenClaw 암호화 기본 설정
# 이 파일을 .env로 복사하여 사용하세요

# 암호화 기본 활성화 (옵트아웃 방식)
# 기본적으로 암호화가 활성화되며, 비활성화하려면 OPENCLAW_ENCRYPTION_DISABLED=true로 설정
OPENCLAW_ENCRYPTION_ENABLED=true

# 암호화 제공자 설정
# local: OS 키체인 사용 (기본값)
# aws-kms: AWS KMS 사용
# azure-keyvault: Azure Key Vault 사용
OPENCLAW_ENCRYPTION_PROVIDER=local

# 암호화 키 ID (KMS 사용 시 필요)
# OPENCLAW_ENCRYPTION_KEY_ID=arn:aws:kms:region:account:key/id

# 키 로테이션 기간 (일 단위, 기본값: 90)
OPENCLAW_ENCRYPTION_ROTATION_DAYS=90

# 참고: OPENCLAW_ENCRYPTION_ENABLED 환경 변수가 설정되지 않은 경우
# 암호화는 기본적으로 활성화됩니다.
# 암호화를 명시적으로 비활성화하려면 OPENCLAW_ENCRYPTION_DISABLED=true로 설정하세요.
